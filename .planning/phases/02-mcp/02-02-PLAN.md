---
phase: 02-mcp
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/routes/missionControl.js
  - backend/src/services/missionService.js
autonomous: false
requirements:
  - AGENT-04
user_setup: []

must_haves:
  truths:
    - "MissionControl面板后端API完整对接，任务队列真实可用"
    - "Agent状态同步到后端，页面刷新数据不丢失"
    - "任务分配功能完整，支持优先级和状态过滤"
  artifacts:
    - path: "backend/src/services/missionService.js"
      provides: "任务和Agent业务逻辑服务"
      min_lines: 120
    - path: "backend/data/mission-store.json"
      provides: "任务和Agent持久化存储"
  key_links:
    - from: "routes/missionControl.js"
      to: "services/missionService.js"
      via: "MissionService 实例"
      pattern: "missionService\\.(createTask|updateTask|assignAgent)"
---

<objective>
MissionControl面板从纯前端Zustand store迁移到真实后端API，任务队列、Agent状态同步、任务分配功能完整可用。
</objective>

<context>
@backend/src/routes/missionControl.js

现有 MissionControl 路由使用内存存储（store对象），页面刷新后数据丢失。
Bug 11: MissionControl后端API完全缺失，组件使用纯前端Zustand store。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 MissionService 业务逻辑层</name>
  <files>
    - backend/src/services/missionService.js
  </files>
  <action>
    创建 `backend/src/services/missionService.js`，实现任务和Agent的业务逻辑：

    ```javascript
    class MissionService {
      constructor(storePath = 'backend/data/mission-store.json') {
        this.storePath = storePath;
        this.tasks = new Map();
        this.agents = new Map();
        this.events = [];
        this.load();
      }

      // 任务CRUD
      createTask(data) { ... }
      getTask(id) { ... }
      listTasks(filters) { ... }
      updateTask(id, updates) { ... }
      deleteTask(id) { ... }

      // 任务执行
      executeTask(id) { ... }
      cancelTask(id) { ... }

      // Agent管理
      registerAgent(data) { ... }
      updateAgent(id, updates) { ... }
      listAgents(filters) { ... }

      // 统计
      getStats() { ... }

      // 持久化
      save() { ... }
      load() { ... }
    }
    ```

    持久化到 `backend/data/mission-store.json`，每次修改后自动保存。
    实现分页、状态过滤、优先级过滤、Agent过滤。
  </action>
  <verify>
    <automated>grep -c "save\|load" backend/src/services/missionService.js && test -f backend/data/mission-store.json</automated>
  </verify>
  <done>
    MissionService 可从文件加载和保存任务/Agent数据
  </done>
</task>

<task type="auto">
  <name>Task 2: 重构 MissionControl 路由使用 MissionService</name>
  <files>
    - backend/src/routes/missionControl.js
  </files>
  <action>
    读取现有 `routes/missionControl.js`（694行）。

    修改路由层使用 MissionService：
    1. 替换顶部内存 store 为 `const missionService = new MissionService()`
    2. 替换所有 `store.tasks.get/set` 为 `missionService.getTask/updateTask`
    3. 替换所有 `store.agents` 为 `missionService.agents`

    保持所有现有端点不变：
    - POST/GET/PUT/DELETE /api/mission/tasks
    - POST /api/mission/tasks/:id/execute
    - POST /api/mission/tasks/:id/cancel
    - POST/GET/PUT/DELETE /api/mission/agents
    - GET /api/mission/stats
    - GET/POST /api/mission/events
    - POST /api/mission/broadcast

    确保事件（events）也通过 MissionService 管理。
  </action>
  <verify>
    <automated>grep -c "missionService" backend/src/routes/missionControl.js</automated>
  </verify>
  <done>
    路由使用 MissionService，任务/Agent数据持久化
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 前端MissionControl组件API对接验证</name>
  <what-built>后端API完整化，前端需要联调验证</what-built>
  <how-to-verify>
    1. 确保后端运行: `cd backend && npm start`
    2. 打开浏览器访问: http://localhost:8080
    3. 打开MissionControl面板（右下角Mission Control按钮）
    4. 创建任务：输入任务名称，点击创建
    5. 检查任务出现在列表中
    6. 刷新页面，任务列表仍然存在（证明持久化成功）
    7. 创建Agent：输入Agent名称和角色
    8. 刷新页面，Agent状态保持
  </how-to-verify>
  <resume-signal>输入 "approved" 或描述问题</resume-signal>
</task>

</tasks>

<verification>
任务创建后刷新页面仍然存在
Agent创建后刷新页面仍然存在
任务执行和取消操作正常
</verification>

<success_criteria>
1. 任务创建后刷新页面，任务列表中仍有该任务
2. Agent注册后刷新页面，Agent列表中仍有该Agent
3. 任务执行状态正确更新
4. 任务取消功能正常
5. 统计接口返回正确数据
</success_criteria>

<output>
创建 `.planning/phases/02-mcp/02-02-SUMMARY.md`
</output>