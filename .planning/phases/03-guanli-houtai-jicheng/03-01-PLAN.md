---
phase: "03"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/routes/admin/stats.js
  - frontend/src/components/admin/AdminDashboard.tsx
autonomous: true
requirements:
  - ADMIN-01
must_haves:
  truths:
    - "AdminDashboard可显示真实统计数据"
    - "总请求数、成功率、平均延迟数据来自MetricsCollector"
    - "工具调用统计来自ToolRegistry"
    - "知识库状态来自RAGService"
  artifacts:
    - path: "backend/src/routes/admin/stats.js"
      provides: "Stats API endpoint"
      min_lines: 150
    - path: "frontend/src/components/admin/AdminDashboard.tsx"
      provides: "Dashboard UI with real data"
      min_lines: 160
  key_links:
    - from: "AdminDashboard.tsx"
      to: "/api/admin/stats"
      via: "fetchApi hook"
      pattern: "fetchApi.*stats"
    - from: "stats.js"
      to: "MetricsCollector"
      via: "require"
      pattern: "MetricsCollector"
    - from: "stats.js"
      to: "ToolRegistry"
      via: "req.app.get"
      pattern: "toolRegistry"
---

<objective>
AdminDashboard组件与Stats API完成联调，使用真实数据替代模拟数据

目的：管理仪表盘展示真实的系统统计信息，包括请求量、成功率、延迟、工具调用排行、知识库状态等
输出：Stats API真实数据在AdminDashboard中正确显示
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/src/routes/admin/stats.js
@frontend/src/components/admin/AdminDashboard.tsx
@backend/src/services/metrics/MetricsCollector.js
@backend/src/services/tools/toolRegistry.js
</context>

<interfaces>
<!-- Backend Stats API Response -->
Backend: GET /api/admin/stats returns:
```json
{
  "success": true,
  "data": {
    "totalRequests": 0,
    "successRate": 1,
    "avgLatency": 0,
    "activeSessions": 0,
    "modelCalls": [],
    "toolCalls": [{ "tool": "string", "count": 0 }],
    "knowledgeBases": [{ "name": "string", "docCount": 0 }]
  }
}
```

<!-- Frontend SystemStats Interface -->
Frontend SystemStats:
```typescript
interface SystemStats {
  totalRequests: number;
  successRate: number;
  avgLatency: number;
  activeSessions: number;
  modelCalls: { model: string; count: number }[];
  toolCalls: { tool: string; count: number }[];
  knowledgeBases: { name: string; docCount: number }[];
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 验证Stats API数据链路</name>
  <files>backend/src/routes/admin/stats.js, backend/src/services/metrics/MetricsCollector.js, backend/src/services/tools/toolRegistry.js</files>
  <action>
    1. 读取 backend/src/routes/admin/stats.js 完整内容
    2. 读取 backend/src/services/metrics/MetricsCollector.js 确认 globalMetrics.getMetrics() 方法存在
    3. 读取 backend/src/services/tools/toolRegistry.js 确认 listTools() 和 getToolStats() 方法存在
    4. 验证 Stats API 的数据流：
       - sessionStats 来自 data/agent-states/ 目录文件读取
       - knowledgeBases 来自 data/rag/ 目录文件读取
       - toolCalls 来自 toolRegistry.listTools() + getToolStats()
       - modelCalls 来自 MetricsCollector.globalMetrics.getMetrics()
    5. 确认 data 目录存在且有正确权限
  </action>
  <verify>
    <automated>node -e "const stats = require('./backend/src/routes/admin/stats'); console.log('Stats module loads:', typeof stats === 'function')"</automated>
  </verify>
  <done>Stats API 数据链路验证完成，所有数据源方法存在</done>
</task>

<task type="auto">
  <name>Task 2: 启动后端并验证Stats端点</name>
  <files>backend/src/routes/admin/stats.js</files>
  <action>
    1. 启动后端服务: cd backend && npm start
    2. 等待服务启动完成（约5秒）
    3. 执行 curl 测试:
       curl -s http://localhost:30000/api/admin/stats | head -c 500
    4. 验证响应格式:
       - 确认 success: true
       - 确认 data 包含 totalRequests, successRate, avgLatency, activeSessions, modelCalls, toolCalls, knowledgeBases
       - 确认 toolCalls 数组存在且格式正确
    5. 如果返回错误，记录错误信息并排查
  </action>
  <verify>
    <automated>curl -s http://localhost:30000/api/admin/stats | node -e "const d=require('fs').readFileSync(0,'utf8'); const j=JSON.parse(d); console.log('success:', j.success, 'hasData:', !!j.data, 'keys:', Object.keys(j.data||{}).join(','))"</automated>
  </verify>
  <done>Stats API 端点返回正确 JSON 格式，包含所有必需字段</done>
</task>

<task type="auto">
  <name>Task 3: 验证前端Dashboard组件数据渲染</name>
  <files>frontend/src/components/admin/AdminDashboard.tsx</files>
  <action>
    1. 读取 frontend/src/components/admin/AdminDashboard.tsx 确认:
       - fetchApi 调用路径为 '/api/admin/stats'
       - 响应数据从 data.data 中提取 (后端返回 { success, data } 格式)
       - SystemStats 接口与后端响应匹配
    2. 检查前端是否正确处理响应格式:
       - 后端返回 { success: true, data: {...} }
       - 前端从 data?.data 提取实际统计数据
    3. 验证字段映射正确:
       - totalRequests -> stats.totalRequests
       - successRate -> stats.successRate (乘以100显示百分比)
       - avgLatency -> stats.avgLatency
       - activeSessions -> stats.activeSessions
       - toolCalls -> stats.toolCalls
       - knowledgeBases -> stats.knowledgeBases
  </action>
  <verify>
    <automated>grep -c "fetchApi.*stats" frontend/src/components/admin/AdminDashboard.tsx && grep -c "data?.data" frontend/src/components/admin/AdminDashboard.tsx</automated>
  </verify>
  <done>AdminDashboard 组件正确调用 /api/admin/stats 并正确解析响应数据</done>
</task>

</tasks>

<verification>
1. 启动后端: cd backend && npm start
2. 启动前端: cd frontend && npm run dev
3. 访问 http://localhost:8080/admin (或对应路由)
4. 验证仪表盘显示:
   - 总请求数卡片有数值（非0或正确显示0）
   - 成功率显示为百分比
   - 平均延迟显示毫秒
   - 工具调用分布有数据（如果有工具调用记录）
   - 知识库状态有数据（如果有知识库）
</verification>

<success_criteria>
1. GET /api/admin/stats 返回 200 状态码
2. 响应 JSON 包含 success: true 和正确结构的 data 对象
3. data 对象包含所有必需字段: totalRequests, successRate, avgLatency, activeSessions, modelCalls, toolCalls, knowledgeBases
4. toolCalls 数组元素格式为 { tool: string, count: number }
5. knowledgeBases 数组元素格式为 { name: string, docCount: number }
6. AdminDashboard 前端组件能正确渲染这些数据
</success_criteria>

<output>
After completion, create `.planning/phases/03-guanli-houtai-jicheng/03-01-SUMMARY.md`
</output>
