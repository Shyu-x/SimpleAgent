---
phase: 04-shengchanji-nengli
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/services/vector/QdrantVectorStore.js
  - backend/src/services/vector/QdrantRouter.js
  - backend/src/config/index.js
  - backend/src/routes/qdrant.js
autonomous: true
requirements:
  - RAG-03
  - RAG-04
must_haves:
  truths:
    - Qdrant集合使用HNSW索引和PQ量化生产参数
    - 数据库连接池配置合理，支持高并发
    - API访问有基础权限验证
  artifacts:
    - path: backend/src/services/vector/QdrantVectorStore.js
      provides: Qdrant生产级配置
      min_lines: 400
    - path: backend/src/routes/qdrant.js
      provides: Qdrant管理API（含HNSW/量化配置）
      min_lines: 300
  key_links:
    - from: QdrantVectorStore.js
      to: Qdrant API
      via: RESTful calls
      pattern: fetch.*qdrant
---

<objective>
完善Qdrant向量数据库生产参数、数据库连接池配置、权限控制
</objective>

<context>
@backend/src/services/vector/QdrantVectorStore.js
@backend/src/services/vector/QdrantRouter.js
@backend/src/routes/qdrant.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Qdrant生产参数配置</name>
  <files>backend/src/services/vector/QdrantVectorStore.js</files>
  <read_first>
    - backend/src/services/vector/QdrantVectorStore.js (完整读取，理解当前集合创建逻辑)
  </read_first>
  <action>
创建生产级Qdrant配置方法：

1. 添加HNSW索引配置：
   - m 参数：16-64（建议32）
   - ef_construction：64-512（建议128）
   - full_scan_threshold：10000

2. 添加PQ量化配置：
   - quantile：0.99（保留99%信息）
   - compression：2（中等压缩）

3. 添加配置方法：
   ```javascript
   async createCollectionWithProductionParams() {
     // 创建带HNSW和量化的集合
     const createResponse = await fetch(`${this.url}/collections/${this.collectionName}`, {
       method: 'PUT',
       headers: this._getHeaders(),
       body: JSON.stringify({
         vectors: {
           size: this.dimension,
           distance: this.distance,
         },
         hnsw_config: {
           m: 32,
           ef_construction: 128,
           full_scan_threshold: 10000,
           on_disk: false,
         },
         quantization: {
           scalar: {
             type: 'quantile',
             quantile: 0.99,
             compression: 'compression16',
           },
         },
       }),
     });
   }
   ```

4. 添加updateCollectionParams方法用于运行时更新HNSW参数

5. 保留原有的createCollection作为兼容，但添加isProduction参数判断
  </action>
  <verify>
    <automated>node -e "const QdrantStore = require('./backend/src/services/vector/QdrantVectorStore'); const store = new QdrantStore(); console.log(typeof store.createCollectionWithProductionParams === 'function' ? 'PASS: createCollectionWithProductionParams exists' : 'FAIL');"</automated>
  </verify>
  <done>
    QdrantVectorStore支持HNSW(m=32,ef_construction=128)和PQ量化(quantile=0.99)生产参数
  </done>
</task>

<task type="auto">
  <name>Task 2: Qdrant管理API增强</name>
  <files>backend/src/routes/qdrant.js</files>
  <read_first>
    - backend/src/routes/qdrant.js (完整读取，理解当前路由结构)
  </read_first>
  <action>
增强Qdrant管理路由，添加生产配置相关端点：

1. 添加HNSW配置端点：
   - GET /api/qdrant/collections/:collection/params - 获取当前HNSW参数
   - PUT /api/qdrant/collections/:collection/params - 更新HNSW参数

2. 添加量化配置端点：
   - GET /api/qdrant/collections/:collection/quantization - 获取量化配置
   - PUT /api/qdrant/collections/:collection/quantization - 更新量化配置

3. 添加优化建议端点：
   - GET /api/qdrant/collections/:collection/optimize - 获取优化建议

4. 添加批量操作端点：
   - POST /api/qdrant/documents/batch - 批量插入（已有，验证是否正常）
   - DELETE /api/qdrant/collections/:collection/points/batch - 批量删除

实现示例：
```javascript
// 更新HNSW参数
router.put('/collections/:collection/params', async (req, res) => {
  const { m, ef_construction, full_scan_threshold } = req.body;
  try {
    const response = await fetch(`${qdrantUrl}/collections/${req.params.collection}`, {
      method: 'PUT',
      headers: getHeaders(),
      body: JSON.stringify({
        hnsw_config: {
          m: m || 32,
          ef_construction: ef_construction || 128,
          full_scan_threshold: full_scan_threshold || 10000,
        },
      }),
    });
    const data = await response.json();
    res.json({ success: true, data });
  } catch (error) {
    res.status(500).json({ success: false, error: error.message });
  }
});
```
  </action>
  <verify>
    <automated>grep -n "ef_construction\|full_scan_threshold\|quantile" backend/src/routes/qdrant.js | head -5</automated>
  </verify>
  <done>
    Qdrant管理API支持HNSW参数更新和量化配置
  </done>
</task>

<task type="auto">
  <name>Task 3: 访问权限控制</name>
  <files>backend/src/middleware/security.js</files>
  <read_first>
    - backend/src/middleware/security.js (如果不存在则创建)
  </read_first>
  <action>
实现基础的API访问权限控制：

1. 创建API Key验证中间件：
   ```javascript
   // 检查X-API-Key头
   const apiKey = req.headers['x-api-key'];
   if (apiKey && apiKey !== process.env.QDRANT_API_KEY) {
     return res.status(403).json({ error: 'Forbidden: Invalid API Key' });
   }
   ```

2. 添加基于角色的访问控制（初步）：
   - admin角色：所有操作
   - user角色：只读操作
   - guest角色：检索操作

3. 在关键路由上应用中间件：
   - POST/PUT/DELETE操作需要admin角色
   - GET操作允许user角色

4. 添加速率限制配置化：
   - 管理接口：10请求/分钟
   - 检索接口：60请求/分钟
   - 批量操作：5请求/分钟
  </action>
  <verify>
    <automated>grep -n "x-api-key\|rateLimit\|role" backend/src/middleware/security.js 2>/dev/null | head -10</automated>
  </verify>
  <done>
    API访问有API Key验证和基础角色权限控制
  </done>
</task>

</tasks>

<verification>
1. Qdrant集合创建时使用HNSW m=32, ef_construction=128参数
2. Qdrant支持PQ量化配置（quantile=0.99）
3. 管理API端点可正常访问并返回正确数据
4. 无API Key的请求被拒绝（403）
</verification>

<success_criteria>
1. Qdrant向量数据库生产参数配置完成（HNSW/量化）✅
2. 数据库连接池优化配置存在 ✅
3. 访问权限控制初步实现 ✅
</success_criteria>

<output>
After completion, create `.planning/phases/04-shengchanji-nengli/04-01-SUMMARY.md`
</output>
