---
phase: 04-shengchanji-nengli
plan: "02"
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - backend/src/services/ragService.js
  - backend/src/services/thinkingChainParser.js
  - backend/src/services/sseService.js
  - backend/src/routes/chat.js
  - frontend/src/components/ChatArea.tsx
  - frontend/src/components/ThinkingChain.tsx
autonomous: false
requirements:
  - RAG-03
  - RAG-04
  - RAG-05
  - CORE-04
  - CORE-05
must_haves:
  truths:
    - RAG检索时自动调用查询改写服务
    - 图片上传后发送base64给后端进行视觉理解
    - 思维链内容从SSE流中解析并传给ThinkingChain组件
  artifacts:
    - path: backend/src/services/ragService.js
      provides: RAG查询改写集成
      min_lines: 450
    - path: backend/src/services/sseService.js
      provides: 思维链SSE流输出
      min_lines: 300
  key_links:
    - from: ChatInput.tsx
      to: /api/chat
      via: fetchStream with image base64
      pattern: attachments.*base64
    - from: SSE Service
      to: ThinkingChain.tsx
      via: thinking type event
      pattern: thinking_delta
---

<objective>
实现RAG查询改写真实集成、多模态图片理解、思维链展示联调
</objective>

<context>
@backend/src/services/ragService.js
@backend/src/services/thinkingChainParser.js
@backend/src/services/sseService.js
@frontend/src/components/ChatInput.tsx
@frontend/src/components/ThinkingChain.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: RAG查询改写集成</name>
  <files>backend/src/services/ragService.js</files>
  <read_first>
    - backend/src/services/ragService.js (完整读取，理解retrieve方法)
    - backend/src/domain/rag/QueryRewriteService.js (理解查询改写接口)
  </read_first>
  <action>
在RAGService.retrieve方法中真实调用查询改写服务：

1. 确保QueryRewriteService已正确实例化：
   - 检查enableQueryRewrite参数
   - 确保llmClient已配置

2. 修改retrieve方法调用rewrite：
   ```javascript
   async retrieve(kbId, query, options = {}) {
     // 查询改写：真实调用LLM补全上下文
     const rewritten = await this.queryRewriteService.rewrite(query, {
       messages: options.messages || [],
       topic: options.topic || ''
     });
     
     // 使用改写后的查询进行检索
     const searchQuery = rewritten.rewritten;
     
     // 语义扩展（可选）
     if (options.enableExpansion) {
       const expanded = await this.queryRewriteService.expand(searchQuery);
       // 合并扩展查询
     }
   }
   ```

3. 确保QueryDecomposeService也在检索中被调用：
   - 复杂查询自动拆分为子查询
   - 并行执行多路检索

4. 在检索结果中返回改写元信息：
   ```javascript
   return {
     results,
     meta: {
       originalQuery: query,
       rewrittenQuery: rewritten.rewritten,
       rewriteType: rewritten.type,
       confidence: rewritten.confidence,
       subQueries: decomposedQueries,
     }
   };
   ```
  </action>
  <verify>
    <automated>grep -n "queryRewriteService.rewrite\|rewritten.rewritten" backend/src/services/ragService.js</automated>
  </verify>
  <done>
    RAG检索时真实调用查询改写服务，返回改写元信息
  </done>
</task>

<task type="auto">
  <name>Task 2: 图片上传视觉理解集成</name>
  <files>backend/src/routes/chat.js</files>
  <read_first>
    - backend/src/routes/chat.js (完整读取，理解当前消息处理)
    - backend/src/services/sseService.js (理解SSE流处理)
  </read_first>
  <action>
实现图片上传到后端进行视觉理解：

1. 修改POST /api/chat路由支持多模态消息：
   - 检查messages中的content是否为数组（OpenAI多模态格式）
   - 支持 { type: "image_url", image_url: { url: "data:image/..." } }

2. 在SSE Service中处理多模态消息：
   ```javascript
   // 检测多模态内容
   const hasImages = messages.some(m => 
     Array.isArray(m.content) || 
     (typeof m.content === 'string' && m.content.includes('data:image'))
   );
   
   // 转换为MiniMax API格式
   const formattedMessages = messages.map(m => {
     if (Array.isArray(m.content)) {
       // 多模态内容直接传递
       return m;
     }
     return m;
   });
   ```

3. 在后端添加图片预处理端点（可选）：
   - POST /api/media/preprocess - 压缩和验证图片
   - 返回处理后的base64或URL

4. 确保ChatInput传递的图片格式正确：
   - attachments中的image已转为base64
   - 发送时格式为 { role: 'user', content: [{ type: 'image_url', image_url: { url: attachment.url } }] }
  </action>
  <verify>
  </verify>
  <done>
    图片上传后作为多模态消息发送给MiniMax API进行视觉理解
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: 思维链展示联调验证</name>
  <files>frontend/src/components/ThinkingChain.tsx, frontend/src/components/ChatArea.tsx</files>
  <read_first>
    - frontend/src/components/ThinkingChain.tsx (理解ThinkingChain组件接口)
    - frontend/src/components/ChatArea.tsx (理解消息渲染逻辑)
  </read_first>
  <action>
验证思维链从SSE到前端展示的完整链路：

1. 确认SSE流中thinking类型事件正确发送：
   - 后端SSE Service在收到thinking_delta时发送 { type: 'thinking', content: '...' }

2. 确认前端apiClient正确解析thinking事件：
   - fetchStream中的onThinking回调被正确调用

3. 确认ChatArea正确传递thinking数据给ThinkingChain组件：
   - 检查是否有useThinkingChain hook的使用
   - 检查ThinkingChain组件是否正确接收chain prop

4. 在浏览器中测试：
   - 发送一条会触发思维链的消息
   - 观察ThinkingChain组件是否显示推理步骤
   - 验证思维链内容与最终回答的分流显示
</action>
  <verify>
    <automated>MISSING - 需要人工验证浏览器行为</automated>
  </verify>
  <done>
    人工验证：思维链在浏览器中正确显示，推理步骤可见
  </done>
</task>

</tasks>

<verification>
1. RAG检索时查询改写服务被真实调用（非mock）
2. 图片作为多模态消息发送，MiniMax返回视觉理解结果
3. 思维链内容从SSE流解析并在ThinkingChain组件中显示
</verification>

<success_criteria>
1. RAG查询改写和拆分的真实集成 ✅
2. 图片上传和思维链展示与后端联调完成 ✅
</success_criteria>

<output>
After completion, create `.planning/phases/04-shengchanji-nengli/04-02-SUMMARY.md`
</output>
