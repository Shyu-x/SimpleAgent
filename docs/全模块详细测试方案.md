# Agent 架构全模块详细测试方案

> 版本: v1.0.0
> 日期: 2026-03-22
> 状态: 规划中

---

## 一、模块清单与测试覆盖矩阵

### 1.1 后端模块清单

| 层级 | 模块 | 文件 | 测试优先级 | 测试类型 | 状态 |
|------|------|------|-----------|---------|------|
| **common** | AppError | `common/errors/AppError.js` | P0 | 单元测试 | ✅ 已完成 |
| **common** | ErrorCodes | `common/errors/AppError.js` | P0 | 单元测试 | ✅ 已完成 |
| **common** | Response | `common/response.js` | P1 | 单元测试 | ⏳ 待补充 |
| **infra** | CircuitBreaker | `infra/circuitBreaker/CircuitBreaker.js` | P0 | 压力测试 | ✅ 已完成 |
| **infra** | CircuitState | `infra/circuitBreaker/CircuitState.js` | P1 | 单元测试 | ⏳ 待补充 |
| **infra** | CircuitEvent | `infra/circuitBreaker/CircuitEvent.js` | P1 | 单元测试 | ⏳ 待补充 |
| **infra** | CircuitBreakerFactory | `infra/circuitBreaker/CircuitBreakerFactory.js` | P1 | 单元测试 | ⏳ 待补充 |
| **infra** | QueueRateLimiter | `infra/rateLimiter/QueueRateLimiter.js` | P0 | 压力测试 | ⏳ 待补充 |
| **infra** | RateLimiterFactory | `infra/rateLimiter/RateLimiterFactory.js` | P1 | 单元测试 | ⏳ 待补充 |
| **infra** | ProbeBufferingCallback | `infra/sse/ProbeBufferingCallback.js` | P1 | 单元测试 | ⏳ 待补充 |
| **infra** | ChatModelClient | `infra/llm/ChatModelClient.js` | P0 | 单元测试 | ⏳ 待补充 |
| **infra** | MiniMaxClient | `infra/llm/MiniMaxClient.js` | P0 | 集成测试 | ⏳ 待补充 |
| **domain** | SearchChannel | `domain/search/SearchChannel.js` | P0 | 单元测试 | ✅ 已完成 |
| **domain** | SearchCoordinator | `domain/search/SearchCoordinator.js` | P0 | 压力测试 | ✅ 已完成 |
| **domain** | VectorSearchChannel | `domain/search/channels/VectorSearchChannel.js` | P1 | 单元测试 | ⏳ 待补充 |
| **domain** | KeywordSearchChannel | `domain/search/channels/KeywordSearchChannel.js` | P1 | 单元测试 | ⏳ 待补充 |
| **domain** | ProcessorChain | `domain/search/ProcessorChain.js` | P1 | 单元测试 | ⏳ 待补充 |
| **domain** | DeduplicationProcessor | `domain/search/postProcessors/DeduplicationProcessor.js` | P1 | 单元测试 | ⏳ 待补充 |
| **domain** | RerankerProcessor | `domain/search/postProcessors/RerankerProcessor.js` | P1 | 单元测试 | ⏳ 待补充 |
| **domain** | ThresholdFilterProcessor | `domain/search/postProcessors/ThresholdFilterProcessor.js` | P1 | 单元测试 | ⏳ 待补充 |
| **domain** | ModelRouter | `domain/model/ModelRouter.js` | P0 | 单元测试 | ⏳ 待补充 |
| **domain** | HealthChecker | `domain/model/HealthChecker.js` | P1 | 单元测试 | ⏳ 待补充 |
| **domain** | IngestionPipeline | `domain/rag/ingestion/IngestionPipeline.js` | P0 | 单元测试 | ⏳ 待补充 |
| **domain** | IngestionNode | `domain/rag/ingestion/IngestionNode.js` | P1 | 单元测试 | ⏳ 待补充 |
| **domain** | ParseNode | `domain/rag/ingestion/nodes/ParseNode.js` | P1 | 单元测试 | ⏳ 待补充 |
| **domain** | ChunkNode | `domain/rag/ingestion/nodes/ChunkNode.js` | P1 | 单元测试 | ⏳ 待补充 |
| **domain** | EmbeddingNode | `domain/rag/ingestion/nodes/EmbeddingNode.js` | P1 | 单元测试 | ⏳ 待补充 |
| **domain** | IndexNode | `domain/rag/ingestion/nodes/IndexNode.js` | P1 | 单元测试 | ⏳ 待补充 |
| **application** | ChatOrchestrator | `application/ChatOrchestrator.js` | P0 | 集成测试 | ⏳ 待补充 |
| **application** | AgentOrchestrator | `application/AgentOrchestrator.js` | P0 | 集成测试 | ⏳ 待补充 |
| **routes** | chat | `routes/chat.js` | P0 | 集成测试 | ✅ 已完成 |
| **routes** | agent | `routes/agent.js` | P0 | 集成测试 | ✅ 已完成 |
| **routes** | search | `routes/search.js` | P1 | 集成测试 | ✅ 已完成 |
| **routes** | rag | `routes/rag.js` | P1 | 集成测试 | ✅ 已完成 |
| **routes** | hitl | `routes/hitl.js` | P1 | 集成测试 | ✅ 已完成 |
| **routes** | a2a | `routes/a2a.js` | P1 | 集成测试 | ✅ 已完成 |

### 1.2 测试覆盖率目标

```
模块总数: 40+
已测试模块: 12
覆盖率: ~30%

目标覆盖率:
- P0 模块: 100%
- P1 模块: 80%
- 整体: 85%
```

---

## 二、Common 层详细测试方案

### 2.1 AppError 单元测试

```javascript
// tests/unit/common/appError.test.js

/**
 * AppError 测试矩阵
 */

// 1. 构造函数测试
test_cases = [
  { name: '默认构造函数', code: 'SYSTEM_INTERNAL_ERROR', httpStatus: 500 },
  { name: '自定义错误码', code: 'AUTH_INVALID_API_KEY', httpStatus: 401 },
  { name: '带 details', code: 'VALIDATION_OUT_OF_RANGE', details: { field: 'age' } },
  { name: '带 suggestion', code: 'BUSINESS_SESSION_NOT_FOUND', suggestion: '请重新创建会话' },
  { name: '带原始错误', code: 'EXTERNAL_MODEL_ERROR', originalError: new Error('timeout') }
]

// 2. 错误码验证
test_cases = [
  { code: 'AUTH_MISSING_API_KEY', expected: { category: 'AUTH', httpStatus: 401 } },
  { code: 'VALIDATION_INVALID_FORMAT', expected: { category: 'VALIDATION', httpStatus: 400 } },
  { code: 'BUSINESS_KB_NOT_FOUND', expected: { category: 'BUSINESS', httpStatus: 404 } },
  { code: 'EXTERNAL_MODEL_TIMEOUT', expected: { category: 'EXTERNAL', httpStatus: 503 } },
  { code: 'SYSTEM_NOT_INITIALIZED', expected: { category: 'SYSTEM', httpStatus: 500 } }
]

// 3. toJSON 序列化测试
test_cases = [
  { input: error, expected: { code: string, message: string, details: object } },
  { input: errorWithOriginal, expected: { originalError: string } }
]

// 4. toHTTPResponse 测试
test_cases = [
  { error: AppError, expected: { statusCode: number, body: object } }
]

// 5. isAppError 类型检查
test_cases = [
  { input: new AppError(...), expected: true },
  { input: new Error(...), expected: false },
  { input: {}, expected: false },
  { input: null, expected: false }
]

// 6. safeAsync 包装测试
test_cases = [
  { fn: () => 'value', expected: { result: 'value' } },
  { fn: () => throw new AppError(...), expected: { throws: true } },
  { fn: () => throw new Error(...), expected: { wrapped: true } }
]
```

### 2.2 Response 单元测试

```javascript
// tests/unit/common/response.test.js

/**
 * 统一响应格式测试
 */

// 1. success 响应测试
test_cases = [
  { data: { id: 1 }, expected: { success: true, data: object } },
  { data: [items], expected: { success: true, data: array, count: number } },
  { data: null, expected: { success: true, data: null } }
]

// 2. error 响应测试
test_cases = [
  { error: AppError, expected: { success: false, error: object } },
  { error: Error, expected: { success: false, error: { code: 'SYSTEM_INTERNAL_ERROR' } } }
]

// 3. paginated 响应测试
test_cases = [
  { items: [], page: 1, pageSize: 10, total: 0,
    expected: { items: [], pagination: { page, pageSize, total, totalPages: 0 } } },
  { items: [10 items], page: 2, pageSize: 10, total: 25,
    expected: { pagination: { totalPages: 3, hasNext: true, hasPrev: true } } }
]

// 4. 响应时间戳测试
test_cases = [
  { data: {}, expected: { timestamp: string } }
]
```

---

## 三、Infra 层详细测试方案

### 3.1 CircuitBreaker 详细测试

```javascript
// tests/stress/circuitBreakerStress.test.js (已完成)

// ========== 测试覆盖清单 ==========

// 1. 状态转换测试
[
  CLOSED → OPEN (失败阈值),
  OPEN → HALF_OPEN (resetTimeout),
  HALF_OPEN → CLOSED (成功阈值),
  HALF_OPEN → OPEN (探测失败),
  CLOSED → OPEN (forceOpen)
]

// 2. 并发测试
[
  100 并发请求处理,
  竞态条件验证,
  状态统计准确性
]

// 3. 超时测试
[
  halfOpenProbeTimeout,
  resetTimeout,
  正常状态无超时
]

// 4. 统计测试
[
  totalSuccesses,
  totalFailures,
  totalProbes,
  totalStateChanges,
  avgLatency
]
```

### 3.2 QueueRateLimiter 详细测试

```javascript
// tests/stress/rateLimiterStress.test.js

/**
 * 限流器压力测试
 */

// 1. 基本限流测试
test_cases = [
  { name: '单窗口限流', windowMs: 60000, maxRequests: 10,
    requests: 15, expected: { allowed: 10, rejected: 5 } },

  { name: '滑动窗口精确计数', windowMs: 1000, maxRequests: 5,
    requests: [0ms, 200ms, 400ms, 600ms, 800ms, 1000ms],
    expected: { allowed: 6, rejected: 0 } },

  { name: '窗口重置后恢复', windowMs: 1000, maxRequests: 3,
    requests: [0ms, 0ms, 0ms, reject, 1001ms, allow],
    expected: { allowed: 4, rejected: 1 } }
]

// 2. Redis ZSET 测试
test_cases = [
  { name: 'ZSET 添加分数正确', score: timestamp, expected: zcard > 0 },
  { name: 'ZSET 过期清理', expectedOldestRemoved: true },
  { name: '并发限流线程安全', concurrency: 100, expected: accurate }
]

// 3. 突发流量测试
test_cases = [
  { name: '突发 10 请求', maxRequests: 5, burst: 10,
    expected: '前 5 通过，后 5 限流' },

  { name: '持续稳定流量', rate: 10/sec, duration: 10s,
    expected: { allowed: 100, rejected: 0 } }
]

// 4. 性能测试
test_cases = [
  { name: '单请求延迟 P99 < 10ms', requests: 1000 },
  { name: '100 并发 QPS > 1000', concurrency: 100, duration: 1s }
]
```

### 3.3 ProbeBufferingCallback 详细测试

```javascript
// tests/unit/sse/probeBuffering.test.js

/**
 * 首包探测装饰器测试
 */

// 1. 首包检测测试
test_cases = [
  { name: '第一个 chunk 应触发首包回调',
    chunks: ['first', 'second', 'third'],
    expected: { firstPackageReceived: true, timing: < 100ms } },

  { name: '空 chunk 不触发首包',
    chunks: ['', 'data'],
    expected: { firstPackageReceived: true } }
]

// 2. 缓冲机制测试
test_cases = [
  { name: '首包前数据应被缓冲',
    behavior: 'buffer',
    expected: { chunks: ['first', 'second'] } },

  { name: '首包后数据应直接传递',
    behavior: 'passthrough',
    expected: { allReceived: true } }
]

// 3. 超时测试
test_cases = [
  { name: '首包超时应触发超时回调',
    timeoutMs: 5000,
    behavior: 'no_data',
    expected: { timeout: true } }
]

// 4. 错误处理测试
test_cases = [
  { name: '错误应中断缓冲',
    errorAt: 'second',
    expected: { errorReceived: true, partialBuffer: ['first'] } }
]
```

---

## 四、Domain 层详细测试方案

### 4.1 SearchChannel 测试矩阵

```javascript
// tests/unit/search/searchChannel.test.js

/**
 * 检索通道测试矩阵
 */

// 1. 基类接口测试
test_cases = [
  { name: 'search() 必须由子类实现',
    fn: () => channel.search('query'),
    expected: { throws: true } },

  { name: 'getType() 返回正确类型',
    expected: { type: string } },

  { name: 'isHealthy() 正确反映健康状态',
    states: [healthy, degraded, unhealthy],
    expected: { healthy: boolean } }
]

// 2. 健康检查测试
test_cases = [
  { name: '连续成功重置失败计数',
    successCount: 5,
    expected: { failureCount: 0, healthy: true } },

  { name: '失败次数超阈值标记不健康',
    failureCount: 5,
    threshold: 5,
    expected: { healthy: false } }
]

// 3. 超时处理测试
test_cases = [
  { name: 'searchWithTimeout 超时抛出错误',
    timeout: 100,
    searchLatency: 200,
    expected: { throws: true, errorType: 'timeout' } },

  { name: '正常返回时 recordSuccess 被调用',
    searchLatency: 10,
    expected: { success: true } }
]
```

### 4.2 SearchCoordinator 测试矩阵

```javascript
// tests/stress/searchStress.test.js (已完成)

// ========== 测试覆盖清单 ==========

// 1. 通道管理
[
  registerChannel,
  unregisterChannel,
  getChannels,
  getChannelsInfo
]

// 2. 检索策略
[
  parallel (并行),
  sequential (串行),
  weighted (加权)
]

// 3. 结果融合 (RRFS)
[
  score calculation,
  rank aggregation,
  multi-source result merging
]

// 4. 后处理器
[
  deduplication,
  reranking,
  threshold filtering
]

// 5. 性能
[
  50 并发 P99 < 100ms,
  100 并发无竞态
]
```

### 4.3 后处理器链测试矩阵

```javascript
// tests/unit/search/postProcessors.test.js

/**
 * 后处理器测试
 */

// 1. DeduplicationProcessor
test_cases = [
  { name: '相同 ID 结果应被去重',
    results: [ {id: 1}, {id: 2}, {id: 1} ],
    expected: { length: 2 } },

  { name: '不同 ID 保留',
    results: [ {id: 1, content: 'a'}, {id: 2, content: 'b'} ],
    expected: { length: 2 } },

  { name: 'Jaccard 相似度去重',
    results: [ {id: 1, content: 'hello world'}, {id: 2, content: 'hello world'} ],
    expected: { length: 1 } }
]

// 2. RerankerProcessor
test_cases = [
  { name: '应使用 LLM 重排结果',
    results: 10 items,
    expected: { reordered: true, topItems: array } },

  { name: '应保留原始分数',
    results: [ {score: 0.9}, {score: 0.8} ],
    expected: { reranked: true, originalScoresPreserved: true } }
]

// 3. ThresholdFilterProcessor
test_cases = [
  { name: '应过滤低分结果',
    results: [ {score: 0.9}, {score: 0.3}, {score: 0.8} ],
    threshold: 0.5,
    expected: { length: 2 } },

  { name: '边界值处理',
    results: [ {score: 0.5} ],
    threshold: 0.5,
    expected: { included: true } }
]
```

### 4.4 ModelRouter 测试矩阵

```javascript
// tests/unit/model/modelRouter.test.js

/**
 * 模型路由器测试
 */

// 1. 客户端注册
test_cases = [
  { name: '注册客户端正确存储', client: MockClient },
  { name: '注销客户端正确移除', client: MockClient },
  { name: '重复注册覆盖' }
]

// 2. 策略选择
test_cases = [
  { name: 'PRIORITY 优先使用主模型',
    strategy: 'PRIORITY',
    primary: 'model-a',
    expected: { used: 'model-a' } },

  { name: 'PRIORITY 主模型故障切换',
    strategy: 'PRIORITY',
    primaryFails: true,
    expected: { used: 'model-b' } },

  { name: 'ROUND_ROBIN 均匀分配',
    strategy: 'ROUND_ROBIN',
    requests: 100,
    expected: { distribution: ~50/50 } },

  { name: 'WEIGHTED_RANDOM 按权重分配',
    strategy: 'WEIGHTED_RANDOM',
    weights: { a: 0.7, b: 0.3 },
    requests: 100,
    expected: { ratioA: '60-80%' } }
]

// 3. 熔断集成
test_cases = [
  { name: '模型故障触发熔断',
    failureCount: 5,
    expected: { circuitBreaker: 'OPEN' } },

  { name: '熔断后自动切换',
    circuitOpen: true,
    expected: { used: 'backup' } }
]

// 4. 健康检查
test_cases = [
  { name: '健康模型返回 true',
    model: 'healthy',
    expected: { healthy: true } },

  { name: '熔断模型返回 false',
    model: 'circuit_open',
    expected: { healthy: false } }
]
```

### 4.5 IngestionPipeline 测试矩阵

```javascript
// tests/unit/rag/ingestionPipeline.test.js

/**
 * 文档入库流水线测试
 */

// 1. 流水线执行
test_cases = [
  { name: '完整流水线执行',
    nodes: [Parse, Clean, Chunk, Embed, Index],
    input: { url: 'https://example.com/doc.pdf' },
    expected: { completed: true, chunksCreated: > 0 } },

  { name: '节点失败不影响后续节点',
    nodes: [Parse, FailNode, Chunk, Embed],
    failAt: 1,
    expected: { chunkCreated: true } }
]

// 2. 节点跳过
test_cases = [
  { name: 'shouldSkip 返回 true 时跳过节点',
    node: { shouldSkip: () => true },
    expected: { skipped: true, notExecuted: true } }
]

// 3. 并行节点
test_cases = [
  { name: '并行组节点同时执行',
    parallelGroup: true,
    expected: { duration: ~max(child durations) } }
]

// 4. 节点重试
test_cases = [
  { name: '节点失败自动重试',
    failCount: 2,
    maxRetries: 3,
    expected: { succeeded: true, retryCount: 2 } },

  { name: '超过最大重试次数报错',
    failCount: 5,
    maxRetries: 3,
    expected: { throws: true } }
]

// 5. 上下文传递
test_cases = [
  { name: '前节点输出作为后节点输入',
    flow: [ {output: 'parsed'}, {expectedInput: 'parsed'} ],
    expected: { correctDataFlow: true } }
]
```

---

## 五、Application 层详细测试方案

### 5.1 ChatOrchestrator 集成测试

```javascript
// tests/integration/chatOrchestrator.test.js

/**
 * 聊天编排器集成测试
 */

// 1. 意图分类
test_cases = [
  { input: '帮我写一段 Python 代码', expected: { intent: 'code_generation' } },
  { input: '今天天气怎么样', expected: { intent: 'conversation' } },
  { input: '搜索一下 AI 最新的进展', expected: { intent: 'search' } },
  { input: '帮我执行 shell 命令', expected: { intent: 'tool_use' } }
]

// 2. 查询改写
test_cases = [
  { input: { query: '它', context: ['上问提到的 AI'] },
    expected: { rewritten: 'AI' } },

  { input: { query: '那个方法', context: ['讨论 Python 装饰器'] },
    expected: { rewritten: 'Python 装饰器' } }
]

// 3. 混合检索
test_cases = [
  { input: { query: 'AI Agent', knowledgeBase: 'default' },
    expected: { results: array, count: > 0 } }
]

// 4. 完整聊天流程
test_cases = [
  { name: '简单对话',
    messages: [{ role: 'user', content: '你好' }],
    expected: { response: string, streamed: true } },

  { name: '多轮对话',
    messages: [
      { role: 'user', content: '我想学习 Python' },
      { role: 'assistant', content: '好的，你想学什么？' },
      { role: 'user', content: '装饰器' }
    ],
    expected: { contextPreserved: true } }
]
```

### 5.2 AgentOrchestrator 集成测试

```javascript
// tests/integration/agentOrchestrator.test.js

/**
 * Agent 编排器集成测试
 */

// 1. 会话管理
test_cases = [
  { name: '创建新会话',
    operation: 'create',
    expected: { sessionId: string, tools: array } },

  { name: '获取已存在会话',
    operation: 'get',
    expected: { sessionId: string, state: object } },

  { name: '会话超时清理',
    operation: 'cleanup',
    timeout: 31min,
    expected: { sessionRemoved: true } }
]

// 2. 检查点管理
test_cases = [
  { name: '保存检查点',
    operation: 'saveCheckpoint',
    expected: { checkpointId: string } },

  { name: '列出检查点',
    operation: 'listCheckpoints',
    expected: { checkpoints: array } },

  { name: '从检查点恢复',
    operation: 'restoreFromCheckpoint',
    expected: { state: object } }
]

// 3. 人机确认
test_cases = [
  { name: '创建确认请求',
    operation: 'requestConfirmation',
    expected: { requestId: string } },

  { name: '批准确认',
    operation: 'respondToConfirmation',
    response: 'approve',
    expected: { executionContinued: true } },

  { name: '拒绝确认',
    operation: 'respondToConfirmation',
    response: 'reject',
    expected: { executionStopped: true } }
]

// 4. 记忆管理
test_cases = [
  { name: '导出记忆',
    operation: 'getMemory',
    expected: { memory: object } },

  { name: '搜索记忆',
    operation: 'searchMemory',
    query: 'Python 装饰器',
    expected: { results: array } }
]
```

---

## 六、Routes 层详细测试方案

### 6.1 API 路由测试矩阵

```javascript
// tests/integration/routes.test.js

/**
 * 路由层集成测试
 */

// 1. /api/chat/send
test_cases = [
  { name: '发送消息返回 SSE 流',
    request: { message: '你好' },
    expected: { status: 200, contentType: 'text/event-stream' } },

  { name: '无效消息格式返回 400',
    request: { message: '' },
    expected: { status: 400, error: true } },

  { name: '未授权返回 401',
    request: { message: 'hi' },
    headers: { authorization: 'invalid' },
    expected: { status: 401 } }
]

// 2. /api/agent/execute
test_cases = [
  { name: 'Agent 执行返回任务结果',
    request: { task: '帮我搜索 AI', sessionId: 'xxx' },
    expected: { status: 200, result: object } },

  { name: '无效 session 返回 404',
    request: { task: 'test', sessionId: 'invalid' },
    expected: { status: 404 } }
]

// 3. /api/search
test_cases = [
  { name: '检索返回结果',
    request: { query: 'AI Agent', channels: ['vector', 'keyword'] },
    expected: { status: 200, results: array } },

  { name: '融合得分正确',
    request: { query: 'test', fusionType: 'RRFS' },
    expected: { results: array, allHaveSources: true } }
]

// 4. /api/hitl/request
test_cases = [
  { name: '创建确认请求',
    request: { type: 'dangerous', action: 'delete_file', params: { path: '/tmp/test' } },
    expected: { status: 200, requestId: string, SSE: true } }
]
```

### 6.2 中间件测试

```javascript
// tests/unit/middleware.test.js

/**
 * 中间件测试
 */

// 1. RateLimiterMiddleware
test_cases = [
  { name: '限流内请求通过',
    requests: 5, limit: 10,
    expected: { allowed: 5, rejected: 0 } },

  { name: '超出限流返回 429',
    requests: 15, limit: 10,
    expected: { allowed: 10, rejected: 5, status: 429 } }
]

// 2. ErrorHandlerMiddleware
test_cases = [
  { name: 'AppError 返回正确格式',
    error: new AppError('AUTH_INVALID_API_KEY'),
    expected: { status: 401, body: { code: 'AUTH_INVALID_API_KEY' } } },

  { name: '未知错误返回 500',
    error: new Error('unknown'),
    expected: { status: 500, code: 'SYSTEM_INTERNAL_ERROR' } }
]

// 3. SecurityMiddleware
test_cases = [
  { name: 'CORS 配置正确',
    request: { origin: 'http://localhost:8080' },
    expected: { cors: true } },

  { name: '安全头存在',
    request: {},
    expected: { headers: { 'X-Content-Type-Options': 'nosniff' } } }
]
```

---

## 七、性能基准测试

### 7.1 基准测试清单

```javascript
// tests/benchmark/runAll.js

const benchmarks = [
  {
    name: 'CircuitBreaker',
    operations: ['execute', 'stateChange', 'reset'],
    load: [10, 50, 100, 500],
    metrics: ['p50', 'p90', 'p99', 'avg', 'qps']
  },
  {
    name: 'SearchCoordinator',
    operations: ['search', 'fuseResults', 'deduplicate'],
    load: [10, 50, 100],
    metrics: ['p50', 'p90', 'p99', 'avg']
  },
  {
    name: 'ModelRouter',
    operations: ['route', 'failover'],
    load: [10, 50, 100],
    metrics: ['p50', 'p90', 'p99', 'avg']
  },
  {
    name: 'IngestionPipeline',
    operations: ['parse', 'chunk', 'embed'],
    load: [1, 5, 10],  // 并发文档数
    metrics: ['throughput', 'avgLatency']
  }
];
```

### 7.2 性能验收标准

| 模块 | 指标 | 目标值 | 当前值 | 状态 |
|------|------|-------|-------|------|
| CircuitBreaker | P99 延迟 | < 10ms | ~8ms | ✅ 达标 |
| SearchCoordinator | P99 延迟 | < 200ms | ~150ms | ✅ 达标 |
| ModelRouter | P99 延迟 | < 50ms | ~45ms | ✅ 达标 |
| IngestionPipeline | 吞吐量 | > 5 docs/s | ~3 docs/s | ⚠️ 待优化 |
| QueueRateLimiter | QPS | > 5000 | ~3000 | ⚠️ 待优化 |

---

## 八、故障注入测试

### 8.1 故障场景矩阵

```javascript
// tests/chaos/chaosScenarios.js

const chaosScenarios = [
  {
    name: '网络延迟注入',
    type: 'network',
    targets: ['MiniMax API', 'Redis', 'Vector DB'],
    levels: [
      { latency: 100, description: '轻微延迟' },
      { latency: 500, description: '明显延迟' },
      { latency: 2000, description: '严重延迟' }
    ]
  },
  {
    name: '服务宕机',
    type: 'crash',
    targets: ['单个模型', 'Redis', '向量数据库'],
    levels: [
      { duration: 5, description: '短暂中断' },
      { duration: 30, description: '中期中断' },
      { duration: 60, description: '长期中断' }
    ]
  },
  {
    name: '资源耗尽',
    type: 'resource',
    targets: ['CPU', 'Memory', 'Connection Pool'],
    levels: [
      { usage: 80, description: '警告级别' },
      { usage: 95, description: '危险级别' },
      { usage: 100, description: '完全耗尽' }
    ]
  }
];
```

### 8.2 恢复时间验收标准

| 故障类型 | 恢复时间目标 | 恢复机制 |
|---------|------------|---------|
| 单模型故障 | < 30s | 熔断 → 切换备份 |
| Redis 故障 | < 10s | 本地缓存降级 |
| 单检索通道故障 | < 1s | 自动切换其他通道 |
| Pipeline 节点故障 | < 5s | 跳过节点继续 |

---

## 九、测试执行计划

### Phase 1: 单元测试补齐 (Day 1)
```
上午 (4h):
- AppError 完整测试
- CircuitBreakerFactory 测试
- ProbeBufferingCallback 测试
- Response 格式化测试

下午 (4h):
- SearchChannel 子类测试
- PostProcessor 链测试
- ModelRouter 策略测试
- HealthChecker 测试
```

### Phase 2: 集成测试完善 (Day 2)
```
上午 (4h):
- ChatOrchestrator 完整流程测试
- AgentOrchestrator 会话管理测试
- 中间件集成测试

下午 (4h):
- Routes API 测试
- HITL 确认流程测试
- A2A 协议测试
```

### Phase 3: 压力与故障测试 (Day 3)
```
上午 (4h):
- 限流器压力测试
- 模型路由压力测试
- 文档入库 Pipeline 压力测试

下午 (4h):
- 故障注入测试
- 恢复时间验证
- 竞态条件测试
```

### Phase 4: 性能调优 (Day 4)
```
上午 (4h):
- 性能基准测试
- 瓶颈分析
- 优化实施

下午 (4h):
- 复测验证
- 测试报告编写
```

---

## 十、测试输出规范

### 10.1 每日测试报告模板

```markdown
# Daily Test Report - YYYY-MM-DD

## 执行摘要
- 测试用例: X 个
- 通过: X 个 (XX%)
- 失败: X 个
- 阻塞: X 个

## 测试详情

### 通过的测试
| 模块 | 用例 | 耗时 | 备注 |
|------|------|------|------|
| ... | ... | ... | ... |

### 失败的测试
| 模块 | 用例 | 错误 | 原因 |
|------|------|------|------|
| ... | ... | ... | ... |

## 问题追踪
| ID | 严重度 | 描述 | 状态 |
|----|--------|------|------|
| ... | ... | ... | ... |

## 明日计划
- [ ] 测试项 1
- [ ] 测试项 2
```

---

**文档状态**: 规划中
**下次审查**: 2026-03-23
**负责人**: AI Team
