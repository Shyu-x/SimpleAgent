# AI Chat 玩具 - 接口文档 (OpenAPI 3.0)

> 版本: v1.0.0
> 日期: 2026-03-21
> 状态: 正式发布

```yaml
openapi: 3.0.3
info:
  title: AI Chat 玩具 API
  description: |
    现代化 AI 对话平台 API，支持对话、Agent、RAG、知识库等功能。

    ## 认证方式

    本 API 使用 Bearer Token 认证，请在请求头中设置：
    ```
    Authorization: Bearer <your_token>
    ```

    ## 错误码

    | 错误码 | 说明 |
    |--------|------|
    | 1xxx | 认证授权类错误 |
    | 2xxx | 参数校验类错误 |
    | 3xxx | 业务逻辑类错误 |
    | 4xxx | 外部依赖类错误 |
    | 5xxx | 系统异常类错误 |

    ## 速率限制

    - 默认限制: 100 请求/分钟
    - Agent 执行: 20 请求/分钟
    - 消息发送: 60 请求/分钟
  version: 1.0.0
  contact:
    name: AI Team
    email: support@example.com
  license:
    name: MIT
    url: https://opensource.org/licenses/MIT

servers:
  - url: http://localhost:30000
    description: 开发环境
  - url: https://api.example.com
    description: 生产环境

tags:
  - name: 对话
    description: 对话相关接口
  - name: 会话
    description: 会话管理接口
  - name: Agent
    description: Agent 执行接口
  - name: RAG
    description: 知识库检索接口
  - name: MCP
    description: MCP 工具接口
  - name: HITL
    description: 人机确认接口
  - name: A2A
    description: Agent 间协作接口
  - name: 系统
    description: 系统管理接口

paths:
  # ==================== 健康检查 ====================
  /health:
    get:
      tags:
        - 系统
      summary: 健康检查
      description: 检查服务健康状态
      operationId: healthCheck
      responses:
        '200':
          description: 服务正常
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
        '503':
          description: 服务不可用
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  # ==================== 对话接口 ====================
  /api/chat/send:
    post:
      tags:
        - 对话
      summary: 发送消息
      description: |
        发送用户消息并获取 AI 回复。支持流式响应（SSE）。

        **流式响应事件**:
        - `message`: 消息片段
        - `tool_call`: 工具调用
        - `tool_result`: 工具结果
        - `done`: 完成
        - `error`: 错误
      operationId: sendMessage
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SendMessageRequest'
            examples:
              简单消息:
                summary: 简单消息
                value:
                  conversationId: "conv_abc123"
                  message: "你好，请介绍一下你自己"
                  modelId: "MiniMax-M2.7-highspeed"
              带参数消息:
                summary: 带参数消息
                value:
                  conversationId: "conv_abc123"
                  message: "用代码实现快速排序"
                  modelId: "MiniMax-M2.7-highspeed"
                  options:
                    temperature: 0.7
                    maxTokens: 4000
                    topP: 0.9
              带附件消息:
                summary: 带附件消息
                value:
                  conversationId: "conv_abc123"
                  message: "请分析这张图片"
                  modelId: "MiniMax-VL-01"
                  attachments:
                    - type: "image"
                      url: "https://example.com/image.jpg"
      responses:
        '200':
          description: 成功获取响应（非流式）
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChatResponse'
        '200':
          description: 成功（流式响应）
          content:
            text/event-stream:
              schema:
                $ref: '#/components/schemas/StreamEvent'
        '400':
          description: 参数校验失败
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationErrorResponse'
        '401':
          description: 未认证
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '429':
          description: 请求过于频繁
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RateLimitErrorResponse'

  /api/chat/cancel:
    post:
      tags:
        - 对话
      summary: 取消消息发送
      description: 取消正在进行的消息生成
      operationId: cancelMessage
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CancelRequest'
      responses:
        '200':
          description: 取消成功
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: "消息已取消"
        '404':
          description: 消息不存在或已完成
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  # ==================== 会话管理 ====================
  /api/conversations:
    get:
      tags:
        - 会话
      summary: 获取会话列表
      description: 获取当前用户的会话列表，支持分页和搜索
      operationId: listConversations
      security:
        - BearerAuth: []
      parameters:
        - name: page
          in: query
          description: 页码
          schema:
            type: integer
            default: 1
            minimum: 1
        - name: pageSize
          in: query
          description: 每页数量
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: keyword
          in: query
          description: 搜索关键词
          schema:
            type: string
        - name: status
          in: query
          description: 会话状态过滤
          schema:
            type: string
            enum: [active, archived, deleted]
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConversationListResponse'
        '401':
          description: 未认证
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

    post:
      tags:
        - 会话
      summary: 创建会话
      description: 创建新的对话会话
      operationId: createConversation
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateConversationRequest'
      responses:
        '201':
          description: 创建成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConversationResponse'
        '400':
          description: 参数错误
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /api/conversations/{conversationId}:
    get:
      tags:
        - 会话
      summary: 获取会话详情
      description: 获取指定会话的详细信息和消息列表
      operationId: getConversation
      security:
        - BearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          description: 会话 ID
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConversationDetailResponse'
        '404':
          description: 会话不存在
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

    delete:
      tags:
        - 会话
      summary: 删除会话
      description: 删除指定的会话（软删除）
      operationId: deleteConversation
      security:
        - BearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          description: 会话 ID
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: 删除成功
        '404':
          description: 会话不存在
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /api/conversations/{conversationId}/messages:
    get:
      tags:
        - 会话
      summary: 获取消息列表
      description: 获取指定会话的消息历史
      operationId: listMessages
      security:
        - BearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          description: 会话 ID
          schema:
            type: string
            format: uuid
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: pageSize
          in: query
          schema:
            type: integer
            default: 50
            maximum: 200
        - name: before
          in: query
          description: 获取此消息 ID 之前的消息
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageListResponse'

  # ==================== Agent 接口 ====================
  /api/agents:
    get:
      tags:
        - Agent
      summary: 获取 Agent 列表
      description: 获取当前用户的 Agent 列表
      operationId: listAgents
      security:
        - BearerAuth: []
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: pageSize
          in: query
          schema:
            type: integer
            default: 20
        - name: status
          in: query
          schema:
            type: string
            enum: [active, inactive, archived]
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentListResponse'

    post:
      tags:
        - Agent
      summary: 创建 Agent
      description: 创建新的 Agent 实例
      operationId: createAgent
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAgentRequest'
      responses:
        '201':
          description: 创建成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentResponse'

  /api/agents/{agentId}:
    get:
      tags:
        - Agent
      summary: 获取 Agent 详情
      operationId: getAgent
      security:
        - BearerAuth: []
      parameters:
        - name: agentId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentResponse'
        '404':
          description: Agent 不存在
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

    put:
      tags:
        - Agent
      summary: 更新 Agent
      operationId: updateAgent
      security:
        - BearerAuth: []
      parameters:
        - name: agentId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAgentRequest'
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentResponse'

    delete:
      tags:
        - Agent
      summary: 删除 Agent
      operationId: deleteAgent
      security:
        - BearerAuth: []
      parameters:
        - name: agentId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: 删除成功

  /api/agents/{agentId}/execute:
    post:
      tags:
        - Agent
      summary: 执行 Agent
      description: |
        执行 Agent 并获取结果。支持流式响应。

        **流式事件**:
        - `start`: 开始执行
        - `thought`: Agent 思考
        - `tool_call`: 工具调用
        - `tool_result`: 工具结果
        - `message`: 输出消息
        - `checkpoint`: 检查点保存
        - `done`: 执行完成
        - `error`: 执行错误
      operationId: executeAgent
      security:
        - BearerAuth: []
      parameters:
        - name: agentId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExecuteAgentRequest'
      responses:
        '200':
          description: 执行成功（非流式）
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExecuteAgentResponse'
        '200':
          description: 执行成功（流式）
          content:
            text/event-stream:
              schema:
                $ref: '#/components/schemas/StreamEvent'
        '404':
          description: Agent 不存在
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /api/agents/{agentId}/runs:
    get:
      tags:
        - Agent
      summary: 获取执行历史
      operationId: listAgentRuns
      security:
        - BearerAuth: []
      parameters:
        - name: agentId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: pageSize
          in: query
          schema:
            type: integer
            default: 20
        - name: status
          in: query
          schema:
            type: string
            enum: [running, completed, failed, cancelled, timeout]
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentRunListResponse'

  /api/agents/{agentId}/cancel:
    post:
      tags:
        - Agent
      summary: 取消 Agent 执行
      operationId: cancelAgentExecution
      security:
        - BearerAuth: []
      parameters:
        - name: agentId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                runId:
                  type: string
                  format: uuid
      responses:
        '200':
          description: 取消成功
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string

  # ==================== RAG/知识库接口 ====================
  /api/kbs:
    get:
      tags:
        - RAG
      summary: 获取知识库列表
      operationId: listKnowledgeBases
      security:
        - BearerAuth: []
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: pageSize
          in: query
          schema:
            type: integer
            default: 20
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KnowledgeBaseListResponse'

    post:
      tags:
        - RAG
      summary: 创建知识库
      operationId: createKnowledgeBase
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateKnowledgeBaseRequest'
      responses:
        '201':
          description: 创建成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KnowledgeBaseResponse'

  /api/kbs/{kbId}:
    get:
      tags:
        - RAG
      summary: 获取知识库详情
      operationId: getKnowledgeBase
      security:
        - BearerAuth: []
      parameters:
        - name: kbId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KnowledgeBaseResponse'

    delete:
      tags:
        - RAG
      summary: 删除知识库
      operationId: deleteKnowledgeBase
      security:
        - BearerAuth: []
      parameters:
        - name: kbId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: 删除成功

  /api/kbs/{kbId}/documents:
    get:
      tags:
        - RAG
      summary: 获取文档列表
      operationId: listDocuments
      security:
        - BearerAuth: []
      parameters:
        - name: kbId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: status
          in: query
          schema:
            type: string
            enum: [pending, processing, completed, failed]
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentListResponse'

    post:
      tags:
        - RAG
      summary: 上传文档
      description: |
        上传文档到知识库，支持 PDF、Word、TXT、Markdown 等格式。

        **上传限制**:
        - 单文件大小: 100MB
        - 支持格式: pdf, doc, docx, txt, md, html
      operationId: uploadDocument
      security:
        - BearerAuth: []
      parameters:
        - name: kbId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/UploadDocumentRequest'
      responses:
        '202':
          description: 上传成功，文档正在处理
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentResponse'

  /api/kbs/{kbId}/documents/{documentId}:
    get:
      tags:
        - RAG
      summary: 获取文档详情
      operationId: getDocument
      security:
        - BearerAuth: []
      parameters:
        - name: kbId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: documentId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentResponse'

    delete:
      tags:
        - RAG
      summary: 删除文档
      operationId: deleteDocument
      security:
        - BearerAuth: []
      parameters:
        - name: kbId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: documentId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: 删除成功

  /api/kbs/{kbId}/documents/{documentId}/status:
    get:
      tags:
        - RAG
      summary: 获取文档处理状态
      operationId: getDocumentStatus
      security:
        - BearerAuth: []
      parameters:
        - name: kbId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: documentId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentStatusResponse'

  /api/kbs/{kbId}/search:
    post:
      tags:
        - RAG
      summary: 检索知识库
      description: |
        在知识库中检索相关内容，支持多路召回和重排序。
      operationId: searchKnowledgeBase
      security:
        - BearerAuth: []
      parameters:
        - name: kbId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SearchRequest'
      responses:
        '200':
          description: 检索成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SearchResponse'

  # ==================== HITL 人机确认接口 ====================
  /api/hitl/requests:
    post:
      tags:
        - HITL
      summary: 创建确认请求
      description: |
        创建需要人工确认的操作请求。用于危险操作、不可逆操作等场景。

        **确认类型**:
        - `dangerous_operation`: 危险操作
        - `irreversible_operation`: 不可逆操作
        - `high_cost_operation`: 高费用操作
        - `external_http_request`: 外部 HTTP 请求
      operationId: createHitlRequest
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateHitlRequest'
      responses:
        '201':
          description: 创建成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HitlRequestResponse'
        '429':
          description: 存在待处理确认请求
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /api/hitl/requests/{requestId}:
    get:
      tags:
        - HITL
      summary: 获取确认请求详情
      operationId: getHitlRequest
      security:
        - BearerAuth: []
      parameters:
        - name: requestId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HitlRequestResponse'

  /api/hitl/requests/{requestId}/respond:
    post:
      tags:
        - HITL
      summary: 响应确认请求
      description: |
        响应 HITL 确认请求。操作员可以批准或拒绝请求。

        **快捷键**:
        - Y: 批准
        - N: 拒绝
        - C: 取消
      operationId: respondHitlRequest
      security:
        - BearerAuth: []
      parameters:
        - name: requestId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HitlResponse'
      responses:
        '200':
          description: 响应成功
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
        '404':
          description: 请求不存在或已过期
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /api/hitl/subscribe/{sessionId}:
    get:
      tags:
        - HITL
      summary: 订阅确认请求
      description: 通过 SSE 实时订阅确认请求状态变化
      operationId: subscribeHitlRequests
      security:
        - BearerAuth: []
      parameters:
        - name: sessionId
          in: path
          required: true
          description: 会话 ID
          schema:
            type: string
      responses:
        '200':
          description: 成功建立 SSE 连接
          content:
            text/event-stream:
              schema:
                $ref: '#/components/schemas/StreamEvent'

  # ==================== A2A Agent 协作接口 ====================
  /api/a2a/agents/register:
    post:
      tags:
        - A2A
      summary: 注册 Agent
      description: 向 A2A 注册中心注册 Agent 实例
      operationId: registerAgent
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/A2AAgentRegistration'
      responses:
        '201':
          description: 注册成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/A2AAgentResponse'

  /api/a2a/agents:
    get:
      tags:
        - A2A
      summary: 获取已注册 Agent 列表
      operationId: listA2AAgents
      security:
        - BearerAuth: []
      parameters:
        - name: capability
          in: query
          description: 按能力筛选
          schema:
            type: string
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/A2AAgentListResponse'

  /api/a2a/agents/{agentId}:
    get:
      tags:
        - A2A
      summary: 获取 Agent 详情
      operationId: getA2AAgent
      security:
        - BearerAuth: []
      parameters:
        - name: agentId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/A2AAgentResponse'
        '404':
          description: Agent 不存在
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /api/a2a/agents/{agentId}/heartbeat:
    post:
      tags:
        - A2A
      summary: 发送心跳
      description: Agent 定期发送心跳以表明在线状态
      operationId: sendAgentHeartbeat
      security:
        - BearerAuth: []
      parameters:
        - name: agentId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: 心跳成功
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  nextHeartbeat:
                    type: integer
                    description: 下次心跳间隔（秒）

  /api/a2a/tasks/send:
    post:
      tags:
        - A2A
      summary: 发送协作任务
      description: 向其他 Agent 发送协作任务请求
      operationId: sendA2ATask
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/A2ATaskRequest'
      responses:
        '202':
          description: 任务已接受
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/A2ATaskResponse'

  /api/a2a/tasks/{taskId}:
    get:
      tags:
        - A2A
      summary: 获取任务状态
      operationId: getA2ATask
      security:
        - BearerAuth: []
      parameters:
        - name: taskId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/A2ATaskResponse'

  /api/a2a/subscribe/{sessionId}:
    get:
      tags:
        - A2A
      summary: 订阅任务更新
      description: 通过 SSE 实时订阅任务状态更新
      operationId: subscribeA2ATasks
      security:
        - BearerAuth: []
      parameters:
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: 成功建立 SSE 连接
          content:
            text/event-stream:
              schema:
                $ref: '#/components/schemas/StreamEvent'

  # ==================== MCP 接口 ====================
  /api/mcp/status:
    get:
      tags:
        - MCP
      summary: 获取 MCP 连接状态
      description: 获取当前 MCP Server 连接状态
      operationId: getMcpStatus
      security:
        - BearerAuth: []
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/McpStatusResponse'

  /api/mcp/connect:
    post:
      tags:
        - MCP
      summary: 连接 MCP Server
      operationId: connectMcp
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - serverUrl
              properties:
                serverUrl:
                  type: string
                  format: uri
                  description: MCP Server URL
                apiKey:
                  type: string
                  description: MCP Server API Key
      responses:
        '200':
          description: 连接成功
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  serverId:
                    type: string

  /api/mcp/tools:
    get:
      tags:
        - MCP
      summary: 获取可用工具列表
      description: 获取当前连接的 MCP Server 支持的工具列表
      operationId: listMcpTools
      security:
        - BearerAuth: []
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/McpToolListResponse'

  /api/mcp/tools/{toolName}/execute:
    post:
      tags:
        - MCP
      summary: 执行 MCP 工具
      description: 执行指定的 MCP 工具
      operationId: executeMcpTool
      security:
        - BearerAuth: []
      parameters:
        - name: toolName
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                arguments:
                  type: object
                  description: 工具参数
      responses:
        '200':
          description: 执行成功
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  result:
                    type: object
                  error:
                    type: string
                  duration:
                    type: integer

  # ==================== 用户接口 ====================
  /api/user/profile:
    get:
      tags:
        - 用户
      summary: 获取用户信息
      operationId: getUserProfile
      security:
        - BearerAuth: []
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserProfileResponse'

    put:
      tags:
        - 用户
      summary: 更新用户信息
      operationId: updateUserProfile
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateUserProfileRequest'
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserProfileResponse'

  /api/user/usage:
    get:
      tags:
        - 用户
      summary: 获取使用统计
      description: 获取用户的使用量统计信息
      operationId: getUserUsage
      security:
        - BearerAuth: []
      parameters:
        - name: startDate
          in: query
          schema:
            type: string
            format: date
        - name: endDate
          in: query
          schema:
            type: string
            format: date
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserUsageResponse'

# ==================== 组件定义 ====================
components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: JWT Token 认证

  schemas:
    # ----- 通用 -----
    ErrorResponse:
      type: object
      required:
        - success
        - code
        - message
      properties:
        success:
          type: boolean
          example: false
        code:
          type: string
          description: 错误码
          example: "2001"
        message:
          type: string
          description: 错误消息
          example: "参数校验失败"
        detail:
          type: string
          description: 错误详情
        traceId:
          type: string
          description: 追踪 ID
        timestamp:
          type: string
          format: date-time
        path:
          type: string
        suggestion:
          type: string
          description: 修复建议
        errors:
          type: array
          items:
            type: object
            properties:
              field:
                type: string
              message:
                type: string
              code:
                type: string

    ValidationErrorResponse:
      allOf:
        - $ref: '#/components/schemas/ErrorResponse'
        - type: object
          properties:
            errors:
              type: array
              items:
                type: object
                properties:
                  field:
                    type: string
                    example: "message"
                  message:
                    type: string
                    example: "消息内容不能为空"
                  code:
                    type: string
                    example: "REQUIRED_FIELD"

    RateLimitErrorResponse:
      allOf:
        - $ref: '#/components/schemas/ErrorResponse'
        - type: object
          properties:
            retryAfter:
              type: integer
              description: 重试间隔（秒）
            blockedBy:
              type: string
              enum: [user, api_key, ip, global]

    HealthResponse:
      type: object
      properties:
        status:
          type: string
          enum: [healthy, degraded, unhealthy]
        timestamp:
          type: string
          format: date-time
        services:
          type: object
          properties:
            database:
              type: string
              enum: [up, down]
            redis:
              type: string
              enum: [up, down]
            llm:
              type: string
              enum: [up, down]

    StreamEvent:
      type: object
      properties:
        event:
          type: string
          enum: [message, tool_call, tool_result, done, error, start, thought, checkpoint]
        data:
          type: object

    # ----- 对话 -----
    SendMessageRequest:
      type: object
      required:
        - message
      properties:
        conversationId:
          type: string
          format: uuid
          description: 会话 ID，为空则创建新会话
        message:
          type: string
          minLength: 1
          maxLength: 100000
          description: 消息内容
        modelId:
          type: string
          default: "MiniMax-M2.7-highspeed"
          description: 模型 ID
        stream:
          type: boolean
          default: true
          description: 是否启用流式响应
        options:
          type: object
          properties:
            temperature:
              type: number
              minimum: 0
              maximum: 2
              default: 0.7
            maxTokens:
              type: integer
              minimum: 1
              maximum: 100000
              default: 4000
            topP:
              type: number
              minimum: 0
              maximum: 1
              default: 0.9
        attachments:
          type: array
          items:
            type: object
            properties:
              type:
                type: string
                enum: [image, file]
              url:
                type: string
                format: uri

    ChatResponse:
      type: object
      properties:
        success:
          type: boolean
        messageId:
          type: string
          format: uuid
        conversationId:
          type: string
          format: uuid
        content:
          type: string
        model:
          type: string
        reasoningContent:
          type: string
          description: 思维链内容 (MiniMax)
        toolCalls:
          type: array
          items:
            $ref: '#/components/schemas/ToolCall'
        usage:
          $ref: '#/components/schemas/Usage'
        latencyMs:
          type: integer
        createdAt:
          type: string
          format: date-time

    ToolCall:
      type: object
      properties:
        id:
          type: string
        type:
          type: string
          example: "function"
        function:
          type: object
          properties:
            name:
              type: string
            arguments:
              type: string
              description: JSON 字符串

    Usage:
      type: object
      properties:
        inputTokens:
          type: integer
        outputTokens:
          type: integer
        totalTokens:
          type: integer

    CancelRequest:
      type: object
      required:
        - messageId
      properties:
        messageId:
          type: string
          format: uuid

    # ----- 会话 -----
    CreateConversationRequest:
      type: object
      properties:
        title:
          type: string
          maxLength: 255
        modelId:
          type: string
          default: "MiniMax-M2.7-highspeed"
        systemPrompt:
          type: string
        options:
          type: object
          properties:
            temperature:
              type: number
            maxTokens:
              type: integer
            topP:
              type: number

    ConversationResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        modelId:
          type: string
        status:
          type: string
        messageCount:
          type: integer
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    ConversationDetailResponse:
      allOf:
        - $ref: '#/components/schemas/ConversationResponse'
        - type: object
          properties:
            messages:
              type: array
              items:
                $ref: '#/components/schemas/MessageResponse'
            metadata:
              type: object

    ConversationListResponse:
      type: object
      properties:
        success:
          type: boolean
        data:
          type: object
          properties:
            items:
              type: array
              items:
                $ref: '#/components/schemas/ConversationResponse'
            pagination:
              $ref: '#/components/schemas/Pagination'

    MessageResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        role:
          type: string
          enum: [user, assistant, system, tool]
        content:
          type: string
        model:
          type: string
        reasoningContent:
          type: string
        toolCalls:
          type: array
          items:
            $ref: '#/components/schemas/ToolCall'
        toolResults:
          type: array
          items:
            type: object
        citations:
          type: array
          items:
            $ref: '#/components/schemas/Citation'
        usage:
          $ref: '#/components/schemas/Usage'
        errorMessage:
          type: string
        latencyMs:
          type: integer
        createdAt:
          type: string
          format: date-time

    Citation:
      type: object
      properties:
        documentId:
          type: string
        chunkId:
          type: string
        score:
          type: number
        content:
          type: string
        metadata:
          type: object

    MessageListResponse:
      type: object
      properties:
        success:
          type: boolean
        data:
          type: object
          properties:
            items:
              type: array
              items:
                $ref: '#/components/schemas/MessageResponse'
            pagination:
              $ref: '#/components/schemas/Pagination'

    Pagination:
      type: object
      properties:
        page:
          type: integer
        pageSize:
          type: integer
        total:
          type: integer
        totalPages:
          type: integer

    # ----- Agent -----
    CreateAgentRequest:
      type: object
      required:
        - name
        - modelId
      properties:
        name:
          type: string
          maxLength: 100
        description:
          type: string
        modelId:
          type: string
        systemPrompt:
          type: string
        tools:
          type: array
          items:
            type: string
        maxIterations:
          type: integer
          default: 10
          minimum: 1
          maximum: 100
        timeoutMs:
          type: integer
          default: 60000
        temperature:
          type: number
          default: 0.7
        memoryEnabled:
          type: boolean
          default: true
        memoryWindow:
          type: integer
          default: 10
        ragEnabled:
          type: boolean
          default: false
        checkpointEnabled:
          type: boolean
          default: true

    UpdateAgentRequest:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        systemPrompt:
          type: string
        tools:
          type: array
          items:
            type: string
        maxIterations:
          type: integer
        timeoutMs:
          type: integer
        temperature:
          type: number
        memoryEnabled:
          type: boolean
        memoryWindow:
          type: integer
        ragEnabled:
          type: boolean
        status:
          type: string
          enum: [active, inactive, archived]

    AgentResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: string
        modelId:
          type: string
        systemPrompt:
          type: string
        tools:
          type: array
          items:
            type: string
        maxIterations:
          type: integer
        timeoutMs:
          type: integer
        temperature:
          type: number
        memoryEnabled:
          type: boolean
        memoryWindow:
          type: integer
        ragEnabled:
          type: boolean
        checkpointEnabled:
          type: boolean
        status:
          type: string
        stats:
          type: object
          properties:
            totalRuns:
              type: integer
            successRate:
              type: number
            avgLatency:
              type: integer
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    AgentListResponse:
      type: object
      properties:
        success:
          type: boolean
        data:
          type: object
          properties:
            items:
              type: array
              items:
                $ref: '#/components/schemas/AgentResponse'
            pagination:
              $ref: '#/components/schemas/Pagination'

    ExecuteAgentRequest:
      type: object
      required:
        - input
      properties:
        input:
          type: string
          description: 输入内容
        stream:
          type: boolean
          default: true
        sessionId:
          type: string
          format: uuid
          description: 会话 ID
        options:
          type: object

    ExecuteAgentResponse:
      type: object
      properties:
        success:
          type: boolean
        runId:
          type: string
          format: uuid
        output:
          type: string
        status:
          type: string
          enum: [completed, failed, timeout]
        iterations:
          type: integer
        toolCalls:
          type: array
          items:
            $ref: '#/components/schemas/ToolCall'
        usage:
          $ref: '#/components/schemas/Usage'
        durationMs:
          type: integer
        errorMessage:
          type: string

    AgentRunListResponse:
      type: object
      properties:
        success:
          type: boolean
        data:
          type: object
          properties:
            items:
              type: array
              items:
                type: object
                properties:
                  id:
                    type: string
                    format: uuid
                  input:
                    type: string
                  output:
                    type: string
                  status:
                    type: string
                  iterations:
                    type: integer
                  durationMs:
                    type: integer
                  startAt:
                    type: string
                    format: date-time
                  endAt:
                    type: string
                    format: date-time
            pagination:
              $ref: '#/components/schemas/Pagination'

    # ----- RAG -----
    CreateKnowledgeBaseRequest:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          maxLength: 100
        description:
          type: string
        embeddingModel:
          type: string
          default: "mxbai-embed-large"
        chunkStrategy:
          type: string
          enum: [fixed, semantic, recursive, markdown]
          default: semantic
        chunkSize:
          type: integer
          default: 500
        chunkOverlap:
          type: integer
          default: 50

    KnowledgeBaseResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: string
        embeddingModel:
          type: string
        chunkStrategy:
          type: string
        chunkSize:
          type: integer
        chunkOverlap:
          type: integer
        status:
          type: string
        documentCount:
          type: integer
        chunkCount:
          type: integer
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    KnowledgeBaseListResponse:
      type: object
      properties:
        success:
          type: boolean
        data:
          type: object
          properties:
            items:
              type: array
              items:
                $ref: '#/components/schemas/KnowledgeBaseResponse'
            pagination:
              $ref: '#/components/schemas/Pagination'

    UploadDocumentRequest:
      type: object
      required:
        - file
      properties:
        file:
          type: string
          format: binary
        title:
          type: string
          description: 文档标题
        description:
          type: string
          description: 文档描述

    DocumentResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        knowledgeBaseId:
          type: string
          format: uuid
        title:
          type: string
        fileName:
          type: string
        fileType:
          type: string
        fileSize:
          type: integer
        status:
          type: string
          enum: [pending, processing, completed, failed]
        progress:
          type: integer
        pageCount:
          type: integer
        wordCount:
          type: integer
        errorMessage:
          type: string
        createdAt:
          type: string
          format: date-time
        processedAt:
          type: string
          format: date-time

    DocumentListResponse:
      type: object
      properties:
        success:
          type: boolean
        data:
          type: object
          properties:
            items:
              type: array
              items:
                $ref: '#/components/schemas/DocumentResponse'
            pagination:
              $ref: '#/components/schemas/Pagination'

    DocumentStatusResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        status:
          type: string
        progress:
          type: integer
        chunks:
          type: integer
        errors:
          type: array
          items:
            type: string
        startedAt:
          type: string
          format: date-time
        completedAt:
          type: string
          format: date-time

    SearchRequest:
      type: object
      required:
        - query
      properties:
        query:
          type: string
          description: 查询文本
        topK:
          type: integer
          default: 5
          minimum: 1
          maximum: 100
        minSimilarity:
          type: number
          default: 0.7
          minimum: 0
          maximum: 1
        includeMetadata:
          type: boolean
          default: true
        channels:
          type: array
          items:
            type: string
            enum: [vector, keyword, hybrid]
          default: ["hybrid"]
        rewrite:
          type: boolean
          default: true
          description: 是否启用问题改写

    SearchResponse:
      type: object
      properties:
        success:
          type: boolean
        results:
          type: array
          items:
            type: object
            properties:
              content:
                type: string
              score:
                type: number
              documentId:
                type: string
                format: uuid
              documentTitle:
                type: string
              chunkId:
                type: string
              metadata:
                type: object
        queryRewrite:
          type: string
          description: 改写后的查询
        totalDuration:
          type: integer
          description: 总耗时 (ms)
        channels:
          type: object
          description: 各通道统计

    # ----- HITL -----
    CreateHitlRequest:
      type: object
      required:
        - sessionId
        - type
        - operation
      properties:
        sessionId:
          type: string
          description: 会话 ID
        type:
          type: string
          enum: [dangerous_operation, irreversible_operation, high_cost_operation, external_http_request]
        operation:
          type: string
          description: 操作类型
        details:
          type: object
          description: 操作详情
        timeout:
          type: integer
          default: 60000
          description: 超时时间 (ms)
        callbackUrl:
          type: string
          format: uri

    HitlRequestResponse:
      type: object
      properties:
        requestId:
          type: string
          format: uuid
        sessionId:
          type: string
        type:
          type: string
        operation:
          type: string
        details:
          type: object
        status:
          type: string
          enum: [pending, approved, rejected, cancelled, expired]
        timeout:
          type: integer
        expiresAt:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time

    HitlResponse:
      type: object
      required:
        - approved
      properties:
        approved:
          type: boolean
        comment:
          type: string
          description: 响应备注

    # ----- A2A -----
    A2AAgentRegistration:
      type: object
      required:
        - agentId
        - name
        - capabilities
      properties:
        agentId:
          type: string
          format: uuid
        name:
          type: string
        capabilities:
          type: array
          items:
            type: string
        endpoint:
          type: string
          format: uri
        metadata:
          type: object

    A2AAgentResponse:
      type: object
      properties:
        agentId:
          type: string
          format: uuid
        name:
          type: string
        capabilities:
          type: array
          items:
            type: string
        endpoint:
          type: string
          format: uri
        status:
          type: string
          enum: [online, offline, busy]
        lastHeartbeat:
          type: string
          format: date-time
        metadata:
          type: object

    A2AAgentListResponse:
      type: object
      properties:
        success:
          type: boolean
        data:
          type: array
          items:
            $ref: '#/components/schemas/A2AAgentResponse'

    A2ATaskRequest:
      type: object
      required:
        - fromAgentId
        - toAgentId
        - taskType
        - input
      properties:
        fromAgentId:
          type: string
          format: uuid
        toAgentId:
          type: string
          format: uuid
        taskType:
          type: string
        input:
          type: object
        callbackUrl:
          type: string
          format: uri

    A2ATaskResponse:
      type: object
      properties:
        taskId:
          type: string
          format: uuid
        status:
          type: string
          enum: [pending, accepted, processing, completed, failed, cancelled]
        estimatedDuration:
          type: integer
          description: 预估耗时 (ms)
        createdAt:
          type: string
          format: date-time

    # ----- MCP -----
    McpStatusResponse:
      type: object
      properties:
        connected:
          type: boolean
        serverUrl:
          type: string
          format: uri
        serverName:
          type: string
        version:
          type: string
        capabilities:
          type: array
          items:
            type: string
        connectedAt:
          type: string
          format: date-time

    McpToolListResponse:
      type: object
      properties:
        success:
          type: boolean
        tools:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              description:
                type: string
              inputSchema:
                type: object

    # ----- 用户 -----
    UserProfileResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        username:
          type: string
        email:
          type: string
        displayName:
          type: string
        avatarUrl:
          type: string
        status:
          type: string
        role:
          type: string
        createdAt:
          type: string
          format: date-time
        lastLoginAt:
          type: string
          format: date-time

    UpdateUserProfileRequest:
      type: object
      properties:
        displayName:
          type: string
        avatarUrl:
          type: string

    UserUsageResponse:
      type: object
      properties:
        period:
          type: object
          properties:
            startDate:
              type: string
              format: date
            endDate:
              type: string
              format: date
        usage:
          type: object
          properties:
            totalTokens:
              type: integer
            inputTokens:
              type: integer
            outputTokens:
              type: integer
            requestCount:
              type: integer
            conversationCount:
              type: integer
            agentRunCount:
              type: integer
        dailyUsage:
          type: array
          items:
            type: object
            properties:
              date:
                type: string
                format: date
              tokens:
                type: integer
              requests:
                type: integer
