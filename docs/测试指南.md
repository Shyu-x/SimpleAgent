# AI Chat 玩具 - 测试指南

> 更新时间: 2026-03-20

## 测试类型

### 1. 单元测试
- **位置**: `backend/tests/unit/`
- **运行**: `node backend/tests/unit/<test-file>.js`
- **覆盖**: HITL检查点、A2A协议、API路由

### 2. 类型检查
- **前端**: `cd frontend && npx tsc --noEmit`
- **后端**: `cd backend && for f in src/**/*.js; do node --check "$f"; done`

### 3. 代码质量
- **ESLint**: `cd frontend && npx eslint src/ --max-warnings=0`
- **目标**: 0 errors, 0 warnings

### 4. 构建测试
- **前端**: `cd frontend && npm run build`
- **后端**: `cd backend && node src/index.js`

## 持续集成

### 质量门槛
- TypeScript: 0 errors
- ESLint: 0 errors
- 构建: 成功
- 后端启动: 成功

### 运行所有检查
```bash
# Frontend
cd frontend && npx tsc --noEmit && npx eslint src/ --max-warnings=0 && npm run build

# Backend
cd backend && for f in src/**/*.js; do node --check "$f"; done && node src/index.js
```

## 测试覆盖目标

| 模块 | 当前覆盖 | 目标 |
|------|---------|------|
| HITL | 基础 | 完整 |
| A2A | 基础 | 完整 |
| Proxy | 基础 | 完整 |
| Agent | 部分 | 完整 |
